pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo "Hello test branch!"
                 sh '''#!/bin/bash
                 git show | grep -i commit | awk '{print $2}' 
                 '''
            }
        }
    }
}
